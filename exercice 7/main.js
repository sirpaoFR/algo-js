let names = [""];
names.push("Vincent", "Paul", "Arthur")
 
 array.forEach(name => {
     name = name + "va a la pêche;" // ou a la place de '= name' on remplace par par '+='
 	console.log(name);
 }

 

